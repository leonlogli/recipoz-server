// Firebase error codes
const EMAIL_ALREADY_EXISTS = 'auth/email-already-exists'
const PHONE_NUMBER_ALREADY_EXISTS = 'auth/phone-number-already-exists'
const USER_NOT_FOUND = 'auth/user-not-found'
const ID_TOKEN_REVOKED = 'auth/id-token-revoked'
const ID_TOKEN_EXPIRED = 'auth/id-token-expired'
const INVALID_ID_TOKEN = 'auth/invalid-id-token'
const ARGUMENT_ERROR = 'auth/argument-error'

export {
  EMAIL_ALREADY_EXISTS,
  PHONE_NUMBER_ALREADY_EXISTS,
  USER_NOT_FOUND,
  ID_TOKEN_EXPIRED,
  ID_TOKEN_REVOKED,
  ARGUMENT_ERROR,
  INVALID_ID_TOKEN
}
